# Compile each program with
gcc3 program.cpp

# gcc3 is a bash function defined in my ~/.bashrc as:
# function gcc3
# {
#     echo "//--------------------------------------------------------------------- Compiling (Optimized version)"
#     local name=${1%.*}
#     c++ -std=c++23 -O3 -Wfatal-errors -Wall -Wextra -Wpedantic $name.cpp -o $name -pthread && strip $name && ./$name
# }

Note:
When the program has several .cpp files, a compile.sh script has been placed to compile.

**Author:** Mario Galindo Queralt, Ph.D.
